#!/usr/bin/env sh
set -e

unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE GIT_COMMON_DIR

echo "[pre-push] typecheck"
pnpm typecheck

echo "[pre-push] biome (lint + format)"
pnpm check

echo "[pre-push] unit tests"
pnpm test:main
