{
  "name": "rebase-git",
  "version": "0.0.2",
  "description": "A fast local Git client for the browser and desktop.",
  "author": "Alexandru Ion <ionalexandru99@users.noreply.github.com>",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ionalexandru99/rebase-git.git"
  },
  "homepage": "https://github.com/ionalexandru99/rebase-git#readme",
  "bugs": {
    "url": "https://github.com/ionalexandru99/rebase-git/issues"
  },
  "type": "module",
  "imports": {
    "#desktop/*": {
      "rebase-source": "./src/apps/desktop/*.ts",
      "types": "./src/apps/desktop/dist/*.d.ts",
      "default": "./src/apps/desktop/dist/*.js"
    },
    "#server/*": {
      "rebase-source": "./src/apps/server/*.ts",
      "types": "./src/apps/server/dist/*.d.ts",
      "default": "./src/apps/server/dist/*.js"
    }
  },
  "bin": {
    "rebase": "./dist/cli.js",
    "rebase-git": "./dist/cli.js"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.18.0 <23.0.0 || >=24.0.0 <25.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b && pnpm --filter @rebase/server prepare:dist && pnpm --filter @rebase/desktop bundle:preload && pnpm --filter @rebase/desktop copy:assets && pnpm build:web",
    "build:desktop-package": "node packaging/build-desktop.ts",
    "build:package": "tsc -b src/contracts src/apps/server && node packaging/build.ts",
    "build:web": "pnpm --filter @rebase/web build:web",
    "check": "biome check .",
    "db:generate": "drizzle-kit generate --config=drizzle.config.ts",
    "dev:electron": "pnpm build && concurrently --kill-others --success first --names web,desktop \"pnpm dev:web --host 127.0.0.1 --strictPort\" \"wait-on http://127.0.0.1:4173 && pnpm --filter @rebase/desktop start -- --renderer-url=http://127.0.0.1:4173\"",
    "dev:server": "node --conditions=rebase-source src/apps/server/cli.ts serve",
    "dev:web": "pnpm --filter @rebase/web dev",
    "package:desktop": "electron-builder --config electron-builder.config.js --publish never",
    "test": "pnpm test:unit",
    "test:boundary": "pnpm test:integration && pnpm test:compatibility && pnpm test:ui && pnpm test:e2e",
    "test:compatibility": "vp test run --project compatibility --reporter=default --reporter=junit --outputFile=tests/.artifacts/vitest/compatibility.xml",
    "test:e2e": "pnpm exec install-electron && pnpm build:desktop-package && pnpm test:e2e:prepared",
    "test:e2e:prepared": "playwright test --project browser --project electron",
    "test:integration": "pnpm build:web && pnpm test:integration:prepared && pnpm test:integration:browser",
    "test:integration:browser": "vp test run --project integration-browser --reporter=default --reporter=junit --outputFile=tests/.artifacts/vitest/integration-browser.xml",
    "test:integration:prepared": "vp test run --project integration --reporter=default --reporter=junit --outputFile=tests/.artifacts/vitest/integration.xml",
    "test:performance": "pnpm build && pnpm test:performance:prepared",
    "test:performance:prepared": "playwright test --config playwright.performance.config.ts",
    "test:release-smoke": "pnpm build:desktop-package && pnpm test:release-smoke:prepared",
    "test:release-smoke:built": "playwright test --config playwright.release-smoke.config.ts",
    "test:release-smoke:prepared": "pnpm package:desktop --dir && pnpm test:release-smoke:built",
    "test:ui": "vp test run --project ui --reporter=default --reporter=junit --outputFile=tests/.artifacts/vitest/ui.xml",
    "test:unit": "vp test run --project unit --reporter=default --reporter=junit --outputFile=tests/.artifacts/vitest/unit.xml",
    "typecheck": "pnpm --recursive run typecheck && tsc --project packaging/tsconfig.json && tsc --project tests/tsconfig.json",
    "validate": "pnpm validate:quality && pnpm validate:integration && pnpm validate:browser && pnpm validate:desktop",
    "validate:browser": "pnpm test:compatibility && pnpm test:integration:browser && pnpm test:ui",
    "validate:desktop": "pnpm exec install-electron && pnpm build:desktop-package && pnpm test:e2e:prepared && pnpm test:release-smoke:prepared",
    "validate:integration": "pnpm build:desktop-package && pnpm test:integration:prepared",
    "validate:quality": "pnpm check && pnpm typecheck && pnpm test:unit && pnpm build",
    "validate:release": "pnpm exec install-electron && pnpm build:desktop-package && pnpm package:desktop && pnpm test:release-smoke:built",
    "prepack": "pnpm build:package"
  },
  "dependencies": {
    "drizzle-orm": "1.0.0-rc.5-169397b",
    "effect": "4.0.0-rc.110",
    "ws": "8.21.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "@playwright/test": "1.62.1",
    "@rebase/contracts": "workspace:*",
    "@rebase/desktop": "workspace:*",
    "@rebase/server": "workspace:*",
    "@rebase/web": "workspace:*",
    "@types/node": "24.13.3",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@types/ws": "8.18.1",
    "@vitest/browser-playwright": "4.1.10",
    "concurrently": "10.0.5",
    "drizzle-kit": "catalog:",
    "electron": "43.4.1",
    "electron-builder": "26.15.3",
    "esbuild": "0.28.2",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "typescript": "7.0.2",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:",
    "vitest-browser-react": "2.2.0",
    "wait-on": "9.1.0"
  }
}
