import { refCommandSlot } from "#web/features/ref-commands/ref-command-slot";
import { RefCommandItems } from "#web-ui/features/ref-commands/components/ref-command-items";

export const RefCommands = {
  Contribute: refCommandSlot.Contribute,
  Items: RefCommandItems,
};
