import { PullNotice } from "#web-ui/features/repository-pull/components/pull-notice";
import { RepositoryPullProvider } from "#web-ui/features/repository-pull/repository-pull-provider";

export const RepositoryPull = {
  Provider: RepositoryPullProvider,
  Notice: PullNotice,
};
